// These files live in src/data/ — edit them directly to change the pre-loaded defaults.
import stipulations  from './stipulations.json';
import sponsors      from './sponsors.json';
import tvDeals       from './tv-deals.json';

export const defaultStipulations = stipulations;
export const defaultSponsors     = sponsors;
export const defaultTVDeals      = tvDeals;
